const url = "http://localhost:5000";

const url_profile = `${url}/profile`
const url_sign_in = `${url}/sign_in`
const url_user = `${url}/users`
export default {
    url_profile,
    url_sign_in,
    url_user
}